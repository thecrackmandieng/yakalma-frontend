services:
  - type: web
    name: yakalma-frontend
    runtime: node
    buildCommand: npm run build:ssr
    startCommand: npm run serve:ssr
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    plan: starter
